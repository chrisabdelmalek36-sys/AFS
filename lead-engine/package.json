{
  "name": "afs-lead-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AFS Trade — Phase 1: daily B2B lead discovery engine (Nardi / outdoor furniture, Egypt)",
  "scripts": {
    "migrate": "tsx src/cli/migrate.ts",
    "seed": "tsx src/cli/seed.ts",
    "pipeline": "tsx src/cli/run-once.ts",
    "leads": "tsx src/cli/show-leads.ts",
    "estimate": "tsx src/cli/estimate.ts",
    "cron": "tsx src/cron/scheduler.ts",
    "dev": "npm run migrate && npm run seed && npm run pipeline -- --mode=sample && npm run leads",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/node-cron": "^3.0.11",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
